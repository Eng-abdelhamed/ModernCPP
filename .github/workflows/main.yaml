name: Testing The Cpp Code

on:
  push:
    branches: ["Tensor/**", "main"]
  pull_request:
    branches: ["main", "Tensor/**"]

jobs:
  run-and-test:
    runs-on: ubuntu-latest
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4


      - name: Setup Cpp
        uses: aminya/setup-cpp@v1
        with:
          compiler: llvm
          vcvarsall: true
          cmake: true
          ninja: true
          vcpkg: true

      - name: Test formatting
        run: |
          echo "Testing Code Style..."
          find . \( -name "*.h" -o -name "*.cpp" \) | xargs clang-format --dry-run --Werror

      
      - name: Testing The cmake
        run: |
          echo "Testing CMake build..."
          mkdir build
          cd build
          cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Release
          cmake --build .

